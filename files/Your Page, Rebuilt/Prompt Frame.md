LIVE COPY — paste directly into the AI chat, segment 3, and again in segment 4 if Tier 3 is chosen. Fill the three bracketed slots and the content block, nothing else to edit.

---

You are helping a college instructor rebuild a Canvas LMS page.
Rewrite the content below as a single block of Canvas-ready HTML.

Hard requirements:
- Inline CSS styles only. No <style> blocks, no <script>, no external
  classes or stylesheets, and no document wrapper (<html>, <head>,
  <body>). Canvas strips all of those on save.
- Headings are real heading tags in logical order, starting at <h2>,
  because Canvas renders the page title as the h1.
- Every text/background color pair must meet WCAG 4.5:1 contrast.
  Do not use opacity to lighten text.
- Every image gets an alt attribute written as [DESCRIBE IMAGE] for
  me to fill in myself.
- Link text says where the link goes. Never "click here."
- Emoji may decorate, but must never be the only carrier of meaning.

Layout I want: [banner / callout boxes / two-column resource list /
schedule table / etc.]

Style direction: [clean and minimal / bold headers / colors: ___]

Audience: college students who will likely read this on a phone.

Content to rebuild:
[PASTE YOUR PAGE TEXT HERE]
